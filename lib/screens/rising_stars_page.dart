import 'package:flutter/material.dart';

class RisingStarsPage extends StatelessWidget {
  const RisingStarsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Rising Stars', style: TextStyle(fontSize: 20)),
    );
  }
}
