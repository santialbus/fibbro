import 'package:flutter/material.dart';

class CuttySharkPage extends StatelessWidget {
  const CuttySharkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cutty Shark', style: TextStyle(fontSize: 20)),
    );
  }
}
