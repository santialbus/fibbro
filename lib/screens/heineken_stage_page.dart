import 'package:flutter/material.dart';

class HeinekenStagePage extends StatelessWidget {
  const HeinekenStagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Heineken Stage', style: TextStyle(fontSize: 20)),
    );
  }
}
