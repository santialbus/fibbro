import 'package:flutter/material.dart';

class RepsolPage extends StatelessWidget {
  const RepsolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Repsol Stage', style: TextStyle(fontSize: 20)),
    );
  }
}
